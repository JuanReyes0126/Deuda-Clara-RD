# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.runtime/private-app.local.*

# env files (MUY IMPORTANTE)
.env
.env.local
.env.*.local

# Vercel — salida local de `vercel build` (Build Output API / prebuilt).
# No versionar: se regenera en cada build y contiene rutas absolutas del equipo.
.vercel/output/
